spring.application.name=casoftWebSpring

# Configura��es do servidor
server.port=8080
server.servlet.context-path=/

# Configura��es do JWT
app.jwt.secret=DOARC_JWT_SECRET_KEY_2024_SUPER_FORTE_MINIMO_32_CARACTERES_PARA_HS256_ALGORITHM
app.jwt.expiration-ms=86400000

# Configura��es de conex�o do pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000

# Configura��es de logging
logging.level.DOARC.mvc=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO

# Configura��es de encoding
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Configura��es de CORS (caso necess�rio)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=false

# Configura��es de desenvolvimento
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
# CORRE��O/MITIGA��O do ClassLoader: Exclui o JAR do PostgreSQL do ClassLoader de rein�cio.
spring.devtools.restart.exclude=postgresql-*.jar

# Configura��es de JSON
spring.jackson.default-property-inclusion=NON_NULL
spring.jackson.serialization.write-dates-as-timestamps=false

# Configura��es do DataSource do Spring
spring.datasource.url=jdbc:postgresql://localhost:5432/DOARC
spring.datasource.username=postgres
spring.datasource.password=postgres123
spring.datasource.driver-class-name=org.postgresql.Driver